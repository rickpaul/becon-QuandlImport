

var parse_date = d3.time.format('%Y-%m-%d').parse;
var LEVEL = 'l';  // unused for now.
var RATE_OF_CHANGE = 'rc'; // unused for now.
var ATTRIBUTE_MODE_DEFAULT = 'level';
var RATE_OF_CHANGE_MODE_DEFAULT = 'absolute' // Deprecated, I think.

var PERCENT_FLAG_DEFAULT = false;
var CHANGE_LINE_DEFAULT = false;

CHART_AXIS_MARGINS = {top: 0, right: 0, bottom: 20, left: 50}; // these margins are actually necessary for axis display


